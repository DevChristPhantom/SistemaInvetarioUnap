<Window x:Class="SisLabTopo.UI.Dashboard.SeleccionarPrestamoActivoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:dashboard="clr-namespace:SisLabTopo.UI.Dashboard"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=dashboard:SeleccionarPrestamoActivoViewModel}"
        Title="Seleccionar Préstamo Activo"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">
    <StackPanel Margin="20">
        <TextBlock Text="Seleccione el préstamo a devolver:"
                   Style="{StaticResource BodyStyle}"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <ComboBox ItemsSource="{Binding PrestamosActivos}"
                  SelectedItem="{Binding PrestamoElegido}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} - {1} ({2:dd/MM HH:mm})">
                                <Binding Path="NombreEstudiante"/>
                                <Binding Path="Curso"/>
                                <Binding Path="FechaPrestamo"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancelar" Command="{Binding CancelarCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,10,0"/>
            <Button Content="Confirmar" Command="{Binding ConfirmarCommand}" Style="{StaticResource SuccessButtonStyle}" Width="110"/>
        </StackPanel>
    </StackPanel>
</Window>
