<Window x:Class="SisLabTopo.UI.Equipos.EquipoFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:equipos="clr-namespace:SisLabTopo.UI.Equipos"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=equipos:EquipoFormViewModel}"
        Title="{Binding TituloVentana}"
        Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">
    <StackPanel Margin="24" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}">

        <TextBlock Text="Código Patrimonial:" Style="{StaticResource BodyStyle}" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBox x:Name="CodigoBox" Text="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding EsEdicion, Converter={StaticResource InverseBoolConverter}}"/>
        <ItemsControl ItemsSource="{Binding (Validation.Errors), ElementName=CodigoBox}" Margin="0,2,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ErrorContent}" Style="{StaticResource SmallStyle}" Foreground="{DynamicResource DangerBrush}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Text="Denominación:" Style="{StaticResource BodyStyle}" FontWeight="Bold" Margin="0,12,0,4"/>
        <TextBox x:Name="DenominacionBox" Text="{Binding Denominacion, UpdateSourceTrigger=PropertyChanged}"/>
        <ItemsControl ItemsSource="{Binding (Validation.Errors), ElementName=DenominacionBox}" Margin="0,2,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ErrorContent}" Style="{StaticResource SmallStyle}" Foreground="{DynamicResource DangerBrush}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Text="Marca:" Style="{StaticResource BodyStyle}" FontWeight="Bold" Margin="0,12,0,4"/>
        <TextBox Text="{Binding Marca, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Modelo:" Style="{StaticResource BodyStyle}" FontWeight="Bold" Margin="0,12,0,4"/>
        <TextBox Text="{Binding Modelo, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="N° de Serie:" Style="{StaticResource BodyStyle}" FontWeight="Bold" Margin="0,12,0,4"/>
        <TextBox Text="{Binding Serie, UpdateSourceTrigger=PropertyChanged}"/>

        <!--
            Etiqueta() es un método de extensión (no una propiedad), así que
            DisplayMemberPath no puede resolverlo: se usa un ItemTemplate con los
            converters de texto de estado/tipo (ver comentario XML de
            TipoEquipoToTextConverter) para no mostrar el nombre crudo del enum.
        -->
        <TextBlock Text="Estado Físico:" Style="{StaticResource BodyStyle}" FontWeight="Bold" Margin="0,12,0,4"/>
        <ComboBox ItemsSource="{Binding EstadosDisponibles}" SelectedItem="{Binding EstadoSeleccionado}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource EstadoEquipoToTextConverter}}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="Categoría:" Style="{StaticResource BodyStyle}" FontWeight="Bold" Margin="0,12,0,4"/>
        <ComboBox ItemsSource="{Binding TiposDisponibles}" SelectedItem="{Binding TipoSeleccionado}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource TipoEquipoToTextConverter}}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="Observaciones:" Style="{StaticResource BodyStyle}" FontWeight="Bold" Margin="0,12,0,4"/>
        <TextBox Text="{Binding Observaciones, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" TextWrapping="Wrap" Height="60" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Text="{Binding MensajeError}"
                   Style="{StaticResource SmallStyle}"
                   FontWeight="Bold"
                   Foreground="{DynamicResource DangerBrush}"
                   TextWrapping="Wrap"
                   Margin="0,12,0,0"
                   Visibility="{Binding MensajeError, Converter={StaticResource StringToVisibilityConverter}}"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar" Command="{Binding CancelarCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="100" Margin="0,0,10,0"/>
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Style="{StaticResource SuccessButtonStyle}" Width="100"/>
        </StackPanel>
    </StackPanel>
</Window>
